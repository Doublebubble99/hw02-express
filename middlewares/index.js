const isValidId = require("./isValidId");
const checkTokenValidate = require("./checkTokenValidate");
const upload = require("./upload");
module.exports = {
  isValidId,
  checkTokenValidate,
  upload,
};
